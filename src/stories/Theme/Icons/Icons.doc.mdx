import { Meta, IconItem, Heading, IconGallery, Wrapper } from '@storybook/blocks';
import { MK_ASSETS } from 'mk-assets/dist/web';

<Meta title="Theme/Icons" />

<Wrapper>
  {Object.entries(MK_ASSETS.icons).map(([key, value]) => (
        <Wrapper key={key}>
          <Heading>{key}</Heading>
          <IconGallery>
            {Object.entries(value).map(([name, Icon]) => (
              <IconItem key={name} name={name}>
                <Icon />
              </IconItem>
            ))}
          </IconGallery>
        </Wrapper>
      ))}
</Wrapper>
