import styled from '@emotion/styled';

export const MKCardSubtitleStyled = styled('h5')`
  width: 100%;
  line-height: 1;
  font-size: var(--mk-font-size-scale-3);
  font-style: italic;
  color: var(--color-neutral-med);
  margin: 0;
`;
